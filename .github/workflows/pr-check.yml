on:
  pull_request:

jobs:
  build-only:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - run: |
          docker buildx build \
            --platform linux/amd64 \
            --progress=plain \
            .
